// Ponto de entrada. `registerRootComponent` faz o que o Expo precisa em cima do
// `AppRegistry` do React Native, incluindo o suporte ao dev client.
import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
